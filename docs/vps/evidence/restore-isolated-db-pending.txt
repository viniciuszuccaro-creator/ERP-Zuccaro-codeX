# Evidência — restore do backup pré-Gate E em Postgres isolado

**Status:** `PENDING_RESTORE_FAILED` (VPS `rc=3` em 2026-09-24T15:40:55Z; script corrigido)
**utc:** 2026-09-24T15:42:00Z

## Último run VPS (sanitizado)

- dump hash OK (`e72ca99b…` / `390275`)
- banco criado: `erp_restore_isolated_20260924_154055`
- `dev_before=postgres|REDACTED_CLUSTER|mig=15`
- `RESTORE_ISOLATED_DB_STATUS=BLOCKED_RESTORE_FAILED rc=3` → isolado dropado
- Causa provável: `CREATE SCHEMA public` / objetos em DB novo; script agora pré-reseta
  `public` só no isolado, filtra `\connect` e imprime excerpt ERROR sanitizado

## Diagnóstico opcional do log antigo (se ainda existir)

```bash
grep -E 'ERROR:|FATAL:' /tmp/restore-isolated-20260924_154055.log | head -40
```

## Bloco para colar na Web Console (baixa para /tmp — não altera checkout DEV)

```bash
set -euo pipefail
SCRIPT_SHA=51fbf6dffe1b42fa9e2204e96d3989ad2eded759
curl -fsSL "https://raw.githubusercontent.com/viniciuszuccaro-creator/ERP-Zuccaro-codeX/${SCRIPT_SHA}/scripts/vps/restore-pre-gate-e-isolated-webconsole.sh" \
  -o /tmp/restore-pre-gate-e-isolated-webconsole.sh
bash /tmp/restore-pre-gate-e-isolated-webconsole.sh
# Em falha: copiar também restore_error_excerpt_sanitized_* / restore_fail_class
# Em sucesso: copiar PASTE_TO_GIT_BEGIN … PASTE_TO_GIT_END
```

**Proibido:** `DATABASE_URL` do DEV; `DROP SCHEMA` no `postgres`; `git pull` de feature na VPS; commit do dump.

```
RESTORE_ISOLATED_DB_STATUS=PENDING_RESTORE_FAILED
last_fail_rc=3
script_fix=51fbf6df
AUTHORIZES_GATES_DEF=NO
EXECUTE_DEF=NO
```
