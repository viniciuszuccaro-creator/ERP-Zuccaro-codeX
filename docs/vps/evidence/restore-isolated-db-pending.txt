# Evidência — restore do backup pré-Gate E em Postgres isolado

**Status:** `PENDING_RESTORE_FAILED` (log_min_messages persistiu após filtro simples; fix -X/admin/agressivo)
**utc:** 2026-09-24T15:49:00Z

## Último run VPS

- `erp_restore_isolated_20260924_154754` · hash dump OK · `dev_untouched` após fail
- `restore_stream_stripped=connect,public_schema,log_gucs` ainda falhou em log_min_messages
- Hipótese: `.psqlrc` / role sem privilégio / linha fora do padrão SET

## Bloco Web Console

```bash
set -euo pipefail
SCRIPT_SHA=419948b6b41d5421111e01ed6ef26ea7ef9476ec
curl -fsSL "https://raw.githubusercontent.com/viniciuszuccaro-creator/ERP-Zuccaro-codeX/${SCRIPT_SHA}/scripts/vps/restore-pre-gate-e-isolated-webconsole.sh" \
  -o /tmp/restore-pre-gate-e-isolated-webconsole.sh
bash /tmp/restore-pre-gate-e-isolated-webconsole.sh
```

Esperado no início: `psql_no_psqlrc=-X`, `restore_db_user=...`, `filtered_log_min_messages_line_count=0`.

```
RESTORE_ISOLATED_DB_STATUS=PENDING_RESTORE_FAILED
AUTHORIZES_GATES_DEF=NO
EXECUTE_DEF=NO
```
