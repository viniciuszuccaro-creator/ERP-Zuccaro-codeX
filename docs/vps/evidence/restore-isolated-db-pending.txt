# Evidência — restore do backup pré-Gate E em Postgres isolado

**Status:** `PENDING_NO_DUMP_ACCESS` (script ainda ausente no checkout da VPS)
**utc:** 2026-09-24T15:36:00Z

## Diagnóstico Web Console (2026-09-24)

```
root@srv1982741:/opt/erp-zuccaro# bash scripts/vps/restore-pre-gate-e-isolated-webconsole.sh
bash: scripts/vps/restore-pre-gate-e-isolated-webconsole.sh: No such file or directory
```

**Causa:** o script existe só na branch `cursor/integracao-sim-33-34-392b` / commit `9ff95c1a`
(PR #35). O tree em `/opt/erp-zuccaro` na VPS **não** foi atualizado (correto — main intocada;
não fazer `git pull` de feature na VPS DEV).

## Bloco para colar na Web Console (baixa para /tmp — não altera checkout DEV)

```bash
# Restore ISOLADO — dump permanece em /opt/erp-zuccaro/backups; DEV=postgres intocado
# Fix: stamp com underscore (hífen quebrava CREATE DATABASE)
set -euo pipefail
SCRIPT_SHA=4c1cd0eeb0307295ab6aab02d181dbc213c85728
curl -fsSL "https://raw.githubusercontent.com/viniciuszuccaro-creator/ERP-Zuccaro-codeX/${SCRIPT_SHA}/scripts/vps/restore-pre-gate-e-isolated-webconsole.sh" \
  -o /tmp/restore-pre-gate-e-isolated-webconsole.sh
bash /tmp/restore-pre-gate-e-isolated-webconsole.sh
# Copiar APENAS o bloco PASTE_TO_GIT_BEGIN … PASTE_TO_GIT_END para o GitHub
```

**Proibido:** `DATABASE_URL` do DEV; `DROP SCHEMA` no `postgres`; `git pull`/`checkout` da feature na VPS; commit do dump.

## Metadados esperados do dump

- path: `/opt/erp-zuccaro/backups/pre-gate-e-20260924-140304.sql`
- bytes=`390275`
- sha256=`e72ca99b453fa6b060b5264f636794b3a601202c18e4185deb12f0020cae3f80`
- banco alvo: `erp_restore_isolated_<stamp>` (≠ `postgres`)
- `dev_untouched=YES`

```
RESTORE_ISOLATED_DB_STATUS=PENDING_NO_DUMP_ACCESS
vps_script_missing_on_checkout=YES
fetch_via_raw_github=READY
AUTHORIZES_GATES_DEF=NO
EXECUTE_DEF=NO
```
