# Evidência — restore do backup pré-Gate E em Postgres isolado

**Status:** `PENDING_NO_DUMP_ACCESS`
**utc:** 2026-09-24T15:30:00Z

## O que foi preparado neste lote

- Guarda anti-DEV: `scripts/vps/assert-isolated-database-url.sh`
  (`ISOLATED_DB_GUARD`; bloqueia `dbname=postgres`; exige `ISOLATED_DATABASE_NAME` + `ALLOW_DROP_SCHEMA_PUBLIC=ISOLATED_ONLY`)
- R07B: `prove-r07b-api-schema-compat.sh` chama a guarda **antes** de `DROP SCHEMA public CASCADE`
- Script Web Console: `scripts/vps/restore-pre-gate-e-isolated-webconsole.sh`
  (cria `erp_restore_isolated_<stamp>`; **não** usa `DATABASE_URL` do DEV; dump permanece na VPS)

## O que foi tentado

- Dump real VPS: `pre-gate-e-20260924-140304.sql` (bytes=`390275`, sha256=`e72ca99b453fa6b060b5264f636794b3a601202c18e4185deb12f0020cae3f80`)
- Localização real: somente `/opt/erp-zuccaro/backups` na VPS (não versionado; correto)
- Neste ambiente Cloud Agent: **dump ausente**; MCP Hostinger-vps **indisponível**; Web Console exige sessão humana autenticada

## O que NÃO conta como restauração de banco

- Cópia de arquivo sintético / self-test filesystem (`validate-isolated-restore.sh --self-test`)
- Self-test da guarda (`assert-isolated-database-url.sh --self-test`)
- PGlite / dump inventado localmente

## Procedimento autorizado — Web Console (colar e devolver PASTE_TO_GIT_*)

```bash
# Na Web Console autenticada da VPS (srv1982741), a partir do repo em /opt/erp-zuccaro:
cd /opt/erp-zuccaro
# Confirmar dump (só metadados):
ls -la backups/pre-gate-e-20260924-140304.sql
sha256sum backups/pre-gate-e-20260924-140304.sql
# Restore ISOLADO (cria erp_restore_isolated_*; DEV=postgres intocado; dump fica na VPS):
bash scripts/vps/restore-pre-gate-e-isolated-webconsole.sh
# Colar no GitHub APENAS o bloco PASTE_TO_GIT_BEGIN … PASTE_TO_GIT_END
```

**Proibido:** apontar `DATABASE_URL` do DEV (`dbname=postgres`) para `prove-r07b-api-schema-compat.sh` ou qualquer `DROP SCHEMA`.

## Critério para fechar esta prova

1. Hash conferido = `e72ca99b453fa6b060b5264f636794b3a601202c18e4185deb12f0020cae3f80` / bytes=`390275`
2. Banco isolado `erp_restore_isolated_*` (≠ `postgres`)
3. `schema_migrations` no isolado (esperado 001–015 / count=15)
4. `dev_untouched=YES`
5. Dump permanece na VPS; Git recebe só evidência sanitizada

```
RESTORE_ISOLATED_DB_STATUS=PENDING_NO_DUMP_ACCESS
vps_dump_used=NO
dev_database_touched=NO
web_console_script=scripts/vps/restore-pre-gate-e-isolated-webconsole.sh
r07b_drop_schema_guard=scripts/vps/assert-isolated-database-url.sh
AUTHORIZES_GATES_DEF=NO
EXECUTE_DEF=NO
```
