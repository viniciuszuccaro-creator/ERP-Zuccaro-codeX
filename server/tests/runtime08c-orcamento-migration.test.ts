import test from 'node:test'; import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises';
test('migration 016 aplica tenant, RLS e constraints estruturais',async()=>{const s=await readFile(new URL('../migrations/016_orcamentos_comercial_360.sql',import.meta.url),'utf8');for(const x of ['assert_orcamento_same_tenant','assert_orcamento_item_same_tenant','FOREIGN KEY(orcamento_id,group_id,empresa_id)','FORCE ROW LEVEL SECURITY','REVOKE ALL ON TABLE orcamentos FROM PUBLIC','desconto<=subtotal','trg_orcamentos_updated_at','Rollback somente com backup/gate'])assert.ok(s.includes(x));});
